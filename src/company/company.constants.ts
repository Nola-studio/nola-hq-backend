/**
 * Product codes for which automated HQ-driven tenant provisioning is implemented.
 * Currently only 'yekoli' (school app gateway hq-provision).
 */
export const PROVISIONABLE_PRODUCT_CODES = new Set<string>(['yekoli']);
